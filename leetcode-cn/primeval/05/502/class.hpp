class Solution {
public:
    int findMaximizedCapital(int k,
        int w,
        vector<int>& profits,
        vector<int>& capital) {
        for (int L = 1 /*, index = */; L < k; ++L) {
            // TODO : 贪心 + 优先队列优化
        }
        return 0;
    }
};
